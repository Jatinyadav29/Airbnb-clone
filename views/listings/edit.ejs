<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= details.title %> Edit</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <header class="site-header">
      <div class="wrap">
        <a href="/listings" class="back-link">← Back to Listings</a>
      </div>
    </header>
    <main class="wrap">
      <article class="detail-card">
        <div class="detail-media">
          <img
            src="<%= (details.image && details.image.url) || details.image || '/images/default.jpg' %>"
            alt="Image of <%= details.title %>"
          />
        </div>
        <div class="detail-body">
          <form
            action="/listings/<%= details._id %>/edit?_method=PATCH"
            method="post"
          >
            <h1 class="detail-title">Edit Listing</h1>

            <label for="title">Title</label>
            <input
              type="text"
              id="title"
              name="title"
              value="<%= details.title %>"
            />

            <label for="location">Location</label>
            <input
              type="text"
              id="location"
              name="location"
              value="<%= details.location %>"
            />

            <label for="country">Country</label>
            <input
              type="text"
              id="country"
              name="country"
              value="<%= details.country %>"
            />

            <label for="description">Description</label>
            <textarea id="description" name="description">
<%= details.description %></textarea
            >

            <label for="price">Price</label>
            <input
              type="number"
              id="price"
              name="price"
              value="<%= details.price %>"
            />
            <div class="button-group">
              <button type="submit">Save</button>
              <a href="/listings" class="button danger">Cancel</a>
            </div>
          </form>
        </div>
      </article>
    </main>
  </body>
</html>
